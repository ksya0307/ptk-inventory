import 'package:flutter/material.dart';

class UpdateRepairForm extends StatelessWidget {
  const UpdateRepairForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Редактирнье"),
    );
  }
}
